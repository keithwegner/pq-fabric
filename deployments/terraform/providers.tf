terraform {
  required_version = ">= 1.5.0"
}

# Phase 9 intentionally does not configure a live cloud provider. Future phases
# can add an AWS provider block and remote backend after operator review.

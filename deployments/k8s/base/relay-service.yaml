apiVersion: v1
kind: Service
metadata:
  name: relay
  namespace: pq-fabric
  labels:
    app.kubernetes.io/component: relay
spec:
  clusterIP: None
  selector:
    app.kubernetes.io/component: relay
  ports:
    - name: http
      port: 8080
      targetPort: http

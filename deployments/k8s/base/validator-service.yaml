apiVersion: v1
kind: Service
metadata:
  name: validator
  namespace: pq-fabric
  labels:
    app.kubernetes.io/component: validator
spec:
  clusterIP: None
  selector:
    app.kubernetes.io/component: validator
  ports:
    - name: http
      port: 8080
      targetPort: http
